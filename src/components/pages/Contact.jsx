import ContactForm from "./sections/ContactForm"

export default function Contact() {
    return(
        <section class="contact">
            <h1>Contact</h1>
            <ContactForm />
        </section>
    )
}